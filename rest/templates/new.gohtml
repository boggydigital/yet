{{define "new"}}
    <!doctype html>
    <html>
    <head>
        <meta charset='UTF-8'>
        <link rel='icon'
              href='data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🔻</text></svg>'
              type='image/svg+xml'/>
        <meta name='viewport' content='width=device-width, initial-scale=1.0'>
        <meta name='color-scheme' content='dark light'>
        <title>🔻 Paste new</title>
        {{template "css-shared"}}
        <style id="css-new">

            label {
                display: block;
                margin-block: var(--normal);
            }

            input[type='text'] {
                width: calc(100% - var(--normal))
            }

            input {
                font-size: var(--fs-larger);
                display: block;
            }

            input[type='submit'] {
                margin-block: var(--normal);
            }

        </style>
    </head>
    <body>

    <a class='video highlight' href='/list'>Back to the watch list</a>

    <form method='get' action='/watch'>
        <label for='v'>Paste or enter YouTube link or video-id:</label>
        <input id='v' name='v' type='text' placeholder='YouTube link or video-id'/>
        <input type='submit'/>
    </form>

    </body>
    </html>
{{end}}